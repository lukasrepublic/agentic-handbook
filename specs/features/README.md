# Features — atomic specs

One atom per `feat-*.md` under `<product>/<domain>/<capability>/`, each with stable
AC-IDs, a `<!-- normative -->` region, and a sibling `acceptance-contract.yaml`.
Templates: the foundry plugin's `context/` kit (see `../../context/README.md`). Worked example: `example/api/ping/`.
