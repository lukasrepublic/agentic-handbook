# Example atom — GET /ping liveness  (feat-example-api-ping)

> **Human-readable intent.** A trivial liveness endpoint. This atom exists only as the
> worked example of the WHAT ladder (human spec → functional spec → frozen contract).
> Delete it once you've authored your own.

<!-- normative -->
## Acceptance criteria

- **AC-PING-1**: `GET /ping` returns HTTP 200 with body exactly `{"ok":true}`.
<!-- /normative -->

## Changelog

- v1.0 Draft. (example)
